{
  "name": "sangut-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server:dev": "nodemon --config nodemon.server.json src/index.ts",
    "server:debug": "nodemon --config nodemon.server.debug.json src/index.ts",
    "repl": "ts-node",
    "lint": "tslint -c tslint.json -p tsconfig.base.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "husky": "^4.2.1",
    "lint-staged": "^10.0.2",
    "nodemon": "^2.0.2",
    "ts-node": "^10.2.1",
    "tslint": "^6.1.2",
    "typescript": "^3.9.10"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts}": "tslint -c tslint.json 'src/**/*.ts'"
  }
}
